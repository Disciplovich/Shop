# Shop

Полноценное веб-приложение для управления магазинами с современным интерфейсом на Vue.js и Spring Boot бэкендом. Система предоставляет полный набор CRUD-операций с возможностью поиска, фильтрации и анализа данных о магазинах.

---
## Технологический стек

### Backend (Spring Boot)
- Java 17+
- Spring Boot 3.x
- MyBatis (для работы с базой данных)
- PostgreSQL (система управления базами данных)
- Springdoc OpenAPI (документация API)
- Lombok (упрощение кода)

### Frontend (Vue.js)
- Vue 3.x (фреймворк)
- Vue Router 4.x (маршрутизация)
- Vuex 4.x (управление состоянием)
- Axios (HTTP-клиент)
- Modern CSS (стилизация)
---
## Требования

### Для запуска Backend:
- JDK 17 или выше
- Maven 3.6+
- PostgreSQL 12+
- Порт 8080 (по умолчанию)

### Для запуска Frontend:
- Node.js 16+
- npm 8+ или yarn
- Порт 3000 (по умолчанию)
---
## Установка и запуск

### 1. Подготовка базы данных

1. Установите PostgreSQL если не установлен
2. Создайте базу данных:
```
   CREATE DATABASE lab6_shop;
```

3. Выполните SQL-скрипт из файла V1__init_shops.sql

### 2. Настройка Backend (Spring Boot)

1. Клонируйте репозиторий или скопируйте файлы проекта
2. Настройте подключение к базе данных в application.properties:
```
   spring.datasource.url=jdbc:postgresql://localhost:5433/lab6_shop
   spring.datasource.username=postgres
   spring.datasource.password=disciple
```
   (Измените параметры в соответствии с вашей настройкой PostgreSQL)

3. Соберите и запустите проект:
```
   mvn clean install
   mvn spring-boot:run
```

   Или запустите через IDE (Lab6MyBatisDbApplication.java)
4. Backend будет доступен по адресу: http://localhost:8080

5. Документация API (Swagger UI): http://localhost:8080/swagger-ui.html

### 3. Настройка Frontend (Vue.js)

1. Перейдите в директорию frontend (если отдельная) или корневую
2. Установите зависимости:
```
   npm install
```

3. Запустите приложение в режиме разработки:
```
   npm run serve
```
4. Frontend будет доступен по адресу: http://localhost:3000

5. Для сборки production версии:
```
   npm run build
```

---
## Функциональность

### Основные возможности:

1. Просмотр магазинов
   - Таблица со всеми магазинами
   - Сортировка по умолчанию по названию
   - Статистика в реальном времени

2. Добавление магазина
   - Форма с валидацией
   - Поддержка всех полей модели
   - Выбор категории из списка

3. Редактирование магазина
   - Загрузка существующих данных
   - Обновление информации
   - Обработка ошибок

4. Удаление магазина
   - Подтверждение действия
   - Мягкое удаление (без физического)
   - Обновление списка

5. Поиск и фильтрация
   - По названию (регистронезависимый)
   - По категории
   - Комбинированный поиск

### Дополнительные функции:

Статистика
   - Общее количество магазинов
   - Количество активных/неактивных
   - Средняя выручка
   - Распределение по категориям
---
## Использование приложения

### 1. Просмотр списка магазинов
1. Откройте главную страницу (http://localhost:3000)
2. Просмотрите таблицу со всеми магазинами
3. Используйте статистические карточки для быстрого анализа
<img width="875" height="416" alt="image" src="https://github.com/user-attachments/assets/00f6af0a-61ab-40c9-a42b-c8f7abf3c8c0" />


### 2. Добавление нового магазина
1. Нажмите кнопку "➕ Добавить магазин"
2. Заполните форму данными
3. Нажмите "Сохранить"
4. Новый магазин появится в списке
<img width="871" height="429" alt="image" src="https://github.com/user-attachments/assets/0691f297-72d8-4fbc-a40e-c7566598f904" />


### 3. Поиск магазинов
1. Используйте поле поиска по названию
2. Выберите категорию для фильтрации
3. Для сброса фильтров нажмите "Сброс"
<img width="974" height="402" alt="image" src="https://github.com/user-attachments/assets/2ece98b9-e4c3-4ba0-947c-a734d626ed9d" />


### 4. Редактирование магазина
1. В таблице нажмите кнопку "✏" напротив нужного магазина
2. Внесите изменения в форме
3. Нажмите "Сохранить изменения"
<img width="719" height="530" alt="image" src="https://github.com/user-attachments/assets/85bdcb2d-3688-4e94-8331-756fbd222d1a" />


### 5. Удаление магазина
1. В таблице нажмите кнопку "🗑" напротив нужного магазина
2. Подтвердите удаление
3. Магазин исчезнет из списка
<img width="774" height="411" alt="image" src="https://github.com/user-attachments/assets/10e76c9e-5de1-4325-9bdf-060259d4f796" />

---
## API Endpoints

### Магазины (Shops):
- GET /api/shops - получить все магазины
- GET /api/shops/{id} - получить магазин по ID
- POST /api/shops - создать новый магазин
- PUT /api/shops/{id} - обновить магазин
- DELETE /api/shops/{id} - удалить магазин

### Поиск (Search):
- GET /api/shops/search/by-name?name={name} - поиск по названию
- GET /api/shops/search/by-category?category={category} - поиск по категории
---
## Модель данных

### Shop (Магазин):
```
public class Shop {
    private Long id;                 // Уникальный идентификатор
    private String name;            // Название магазина
    private String address;         // Адрес
    private String phone;           // Телефон
    private String email;           // Email
    private LocalDate openingDate;  // Дата открытия
    private Boolean active;         // Статус активности
    private Integer employeeCount;  // Количество сотрудников
    private Double area;            // Площадь (м²)
    private String category;        // Категория
    private Double revenue;         // Выручка (руб.)
}
```

### Категории магазинов:
- продукты
- спорттовары
- электроника
- косметика
- одежда
- другое
---
## Архитектура проекта

### Backend структура:
```
src/main/java/ru/miit/lab6mybatisdb/
├── config/              # Конфигурации
├── controllers/         # REST контроллеры
├── mapper/             # MyBatis мапперы
├── model/              # Модели данных
├── service/            # Бизнес-логика
└── Lab6MyBatisDbApplication.java
```

### Frontend структура:
```
src/
├── components/         # Vue компоненты
│   ├── ShopEdit.vue   # Редактирование магазина
│   ├── ShopForm.vue   # Форма создания магазина
│   ├── ShopList.vue   # Список магазинов
│   └── ShopSearch.vue # Поиск магазинов
├── router/            # Маршрутизация
├── services/          # API сервисы
├── store/             # Vuex хранилище
├── App.vue            # Главный компонент
└── main.js            # Точка входа
```
---
## Решение проблем

### 1. Backend не запускается
- Проверьте версию Java (должна быть 17+)
- Проверьте подключение к PostgreSQL
- Убедитесь, что порт 8080 свободен
- Проверьте логи приложения

### 2. Frontend не подключается к Backend
- Убедитесь, что backend запущен на порту 8080
- Проверьте настройки прокси в vue.config.js
- Проверьте CORS настройки (если разные домены)
- Используйте инструменты разработчика в браузере для проверки запросов

### 3. Ошибки базы данных
- Проверьте, создана ли база данных lab6_shop
- Убедитесь, что таблица shops существует
- Проверьте права доступа пользователя PostgreSQL
- Проверьте SQL-скрипт инициализации

### 4. Ошибки сборки Frontend
- Убедитесь, что Node.js версии 16+
- Удалите node_modules и выполните npm install заново
- Проверьте версии зависимостей в package.json
